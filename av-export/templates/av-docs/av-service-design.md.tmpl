---
template: docs/SERVICE-DESIGN.md.tmpl
service: "{{SERVICE_NAME}}"
module: "{{MODULE}}"
---
# {{SERVICE_NAME}} 상세 설계

> {{CATEGORY}} / {{DOMAIN}} | 작성일: {{DATE}}

## 개요

- **서비스명**: {{SERVICE_NAME}}
- **모듈**: {{MODULE}}
- **책임**: (서비스 책임 범위 기술)

## API 목록

| Method | Path | 설명 | 인증 |
|--------|------|------|------|
| GET | /api/{{MODULE}} | 목록 조회 | Required |
| GET | /api/{{MODULE}}/:id | 상세 조회 | Required |
| POST | /api/{{MODULE}} | 등록 | Required |
| PATCH | /api/{{MODULE}}/:id | 수정 | Required |
| DELETE | /api/{{MODULE}}/:id | 삭제 | Required |

## DB 스키마

```prisma
model {{MODULE}} {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

## NATS 이벤트

| Subject | 방향 | Payload | 설명 |
|---------|------|---------|------|
| `{{MODULE}}.created` | Publish | `{ id }` | 생성 이벤트 |
| `{{MODULE}}.updated` | Publish | `{ id }` | 수정 이벤트 |

## 의존 서비스

- `auth-backend`: 인증/인가
- `tenant-backend`: 테넌트 스키마 분리

## 비즈니스 규칙

1. (핵심 비즈니스 규칙 기술)
2. (유효성 검증 규칙 기술)

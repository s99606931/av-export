---
name: {{agent-name}}
description: |
  {{role-description}}
  트리거: {{trigger-conditions}}
autovibe: true
version: "1.0"
created: "{{created-date}}"
group: {{group}}
tier: {{tier-or-null}}
inherits: {{inherits-or-null}}
tools: [Read, Glob, Grep, Write, Edit, Bash]
model: sonnet
scope: "{{scope-glob-pattern}}"
---

# {{agent-name}}

## 역할 — {{role-description}}

{{agent-purpose-paragraph}}

## 상속 컨텍스트

{{#if inherits}}
> 이 컴포넌트는 `{{inherits}}`를 상속합니다.
> 작업 시작 전 부모 파일을 Read하여 공통 로직을 확인하세요: `.claude/agents/{{inherits}}.md`

| 항목 | 부모 값 | 이 컴포넌트 값 |
|------|---------|--------------|
| scope | {{parent-scope}} | {{this-scope}} |
| {{override-item}} | {{parent-value}} | {{this-value}} |

## 공통 로직 (부모에서 상속)

[부모의 모든 로직 그대로 실행 — `{{inherits}}.md` 참조]

## {{group}} 전용 추가 로직

{{group-specific-logic}}

{{else}}
> 이 컴포넌트는 독립(base) 에이전트입니다.
{{/if}}

## 전용 로직

{{custom-logic-steps}}

## 실행 프로토콜 참조

- 시작/종료 프로토콜: `.claude/docs/av-claude-code-spec/topics/protocols.md`
- 감사 규칙: `.claude/docs/av-claude-code-spec/topics/audit-rules.md`
- Frontmatter 명세: `.claude/docs/av-claude-code-spec/topics/frontmatter-spec.md`
